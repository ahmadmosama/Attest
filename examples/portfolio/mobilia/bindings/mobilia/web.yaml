# mobilia, web. GENERATED from the live app by tools/generate-smoke.mjs.
#
# Bound by ROLE and NAME rather than by test id, because this app has none.
# That is a weaker binding: renaming a visible label breaks it, where a test
# id would not have. It is what the app supports today, and it is honest
# about that rather than pretending to a stability it does not have.
surface: web

elements:
  text:primary_heading: { role: heading, name: "a calm way to redesign a room." }
  button:start_a_room: { role: button, name: "start a room" }
  button:i_m_a_carpenter: { role: button, name: "I'm a carpenter" }
  link:design: { role: link, name: "design" }
  link:jobs: { role: link, name: "jobs" }

screens:
  screen:home: { path: "/", ready: { role: heading, name: "a calm way to redesign a room." } }
  screen:design: { path: "/upload", ready: { role: heading, name: "upload + quiz" } }
  screen:jobs: { path: "/jobs", ready: { role: heading, name: "posted to carpenters" } }

states: {}
