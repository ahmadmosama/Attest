# khayat, web. GENERATED from the live app by tools/generate-smoke.mjs.
#
# Bound by ROLE and NAME rather than by test id, because this app has none.
# That is a weaker binding: renaming a visible label breaks it, where a test
# id would not have. It is what the app supports today, and it is honest
# about that rather than pretending to a stability it does not have.
surface: web

elements:
  text:primary_heading: { role: heading, name: "the tailor in your pocket" }
  button:eg_egp: { role: button, name: "EG EGP" }
  button:ar: { role: button, name: "AR" }
  button:enter_as_customer: { role: button, name: "enter as customer" }
  link:enter_as_customer: { role: link, name: "enter as customer" }
  link:enter_as_tailor: { role: link, name: "enter as tailor" }

screens:
  screen:home: { path: "/", ready: { role: heading, name: "the tailor in your pocket" } }
  screen:enter_as_customer: { path: "/customer/discover", ready: { role: heading, name: "considered clothing, for you" } }
  screen:enter_as_tailor: { path: "/tailor/inbox", ready: { role: heading, name: "briefs awaiting your call" } }

states: {}
