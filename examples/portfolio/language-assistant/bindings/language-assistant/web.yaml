# language-assistant, web. GENERATED from the live app by tools/generate-smoke.mjs.
#
# Bound by ROLE and NAME rather than by test id, because this app has none.
# That is a weaker binding: renaming a visible label breaks it, where a test
# id would not have. It is what the app supports today, and it is honest
# about that rather than pretending to a stability it does not have.
surface: web

elements:
  text:primary_heading: { role: heading, name: "not a language app. a language friend." }
  link:meet_your_language_friend: { role: link, name: "meet your language friend" }

screens:
  screen:home: { path: "/", ready: { role: heading, name: "not a language app. a language friend." } }
  screen:meet_your_language_friend: { path: "/onboarding", ready: { role: heading, name: "hey! what should Alex call you?" } }

states: {}
