# Database capture uses a direct PostgreSQL session connection on port 5432, never the Supabase transaction pooler on 6543.
# ATTEST_DB_URL=postgres://attest_user:fake_password@db.example.test:5432/app_test
